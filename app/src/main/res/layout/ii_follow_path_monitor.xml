<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:id="@+id/follow_path_monitor"
    android:background="@drawable/button_border_bianco"
    android:visibility="invisible"
    android:paddingLeft="8dp"
    android:paddingRight="8dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <View
        android:id="@+id/follow_path_alarm_icon"
        android:background="@drawable/divieto64"
        android:visibility="gone"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_marginEnd="6dp"
        android:layout_marginRight="6dp"/>
    <TextView
        android:textStyle="bold"
        android:textColor="#ff0000"
        android:id="@+id/follow_path_monitor_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:text="@string/followPathAlarm"
        android:gravity="center_vertical"/>
</LinearLayout>
